# 项目目录整理记录（2026-07-18）

## 整理内容

### 1. 根目录清理

将 20 个零散的工具脚本按用途分类搬迁到 `tools/` 目录：

- **`tools/analyze/`**（9 个）：模板分析、标签检查、预处理器行为观察脚本
- **`tools/fix/`**（8 个）：模板修复系列脚本（v2~v4 版本演进 + 空格修复 + 标签更正）
- **`tools/dump/`**（3 个）：模板预处理 XML 导出脚本

删除生成产物：
- `jinja_source.txt`、`jinja_source.xml`、`jinja_source2.xml`（调试 dump 输出）
- `template_analysis.txt`、`template_tags.txt`（模板分析输出）
- `test_output.docx`、`test_output_1~3.docx`（测试脚本渲染产物）
- `templates_docx/*.bak*`（模板修复中间备份）

删除缓存目录：
- `__pycache__/`、`.pytest_cache/`

### 2. 更新 .gitignore

完善忽略规则，适配新的 `tools/` 目录结构和生成产物路径。

### 3. 更新 README.md

- 补充"互提资料生成器"完整功能描述
- 技术栈修正：前端从"原生 JS"更新为 Vue 3 + Vue Router
- 更新项目结构目录树
- 新增仓库变量参考、标签选型规则、排障入口等实用章节
- 新增测试运行命令

### 4. 新增 tools/README.md

说明 tools/ 下三个子目录中每个脚本的用途，提醒运行时的路径注意点。

## 整理原则

- 不修改任何运行时代码（app.py、static/、templates/ 原封不动）
- 不移动 templates_docx/ 中的正式模板文件
- 保留 `tools/` 下的所有脚本（即使部分已有后续版本覆盖，作为问题排查历史保留其代码逻辑）
- 模板 `.bak` 备份删除前确认当前正式模板可正常工作

## 当前项目状态

根目录从 35+ 个文件精简到 ~20 个文件，目录结构清晰直观。
